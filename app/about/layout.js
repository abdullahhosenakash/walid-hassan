export const metadata = {
  title: 'About - Walid Hassan',
  description: "This is Walid Hassan's about page of the portfolio website"
};

const AboutLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default AboutLayout;
