export const metadata = {
  title: 'Update About - Walid Hassan',
  description:
    "This is Walid Hassan's update about page of the portfolio website"
};

const UpdateAboutLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateAboutLayout;
