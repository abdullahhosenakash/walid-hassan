export const metadata = {
  title: 'Update Certifications - Walid Hassan',
  description:
    "This is Walid Hassan's update certifications page of the portfolio website"
};

const UpdateCertificationsLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateCertificationsLayout;
