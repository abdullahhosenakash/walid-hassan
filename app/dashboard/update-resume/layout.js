export const metadata = {
  title: 'Update Resume - Walid Hassan',
  description:
    "This is Walid Hassan's update resume page of the portfolio website"
};

const UpdateResumeLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateResumeLayout;
