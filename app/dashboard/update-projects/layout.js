export const metadata = {
  title: 'Update Projects - Walid Hassan',
  description:
    "This is Walid Hassan's update projects page of the portfolio website"
};

const UpdateProjectsLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateProjectsLayout;
