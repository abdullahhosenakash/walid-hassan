export const metadata = {
  title: 'Update Contact - Walid Hassan',
  description:
    "This is Walid Hassan's update contact page of the portfolio website"
};

const UpdateContactLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateContactLayout;
