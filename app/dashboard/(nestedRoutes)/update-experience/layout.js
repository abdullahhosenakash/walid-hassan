export const metadata = {
  title: 'Update Experience - Walid Hassan',
  description:
    "This is Walid Hassan's update experience page of the portfolio website"
};

const UpdateExperienceLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default UpdateExperienceLayout;
