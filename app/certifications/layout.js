export const metadata = {
  title: 'Certifications - Walid Hassan',
  description:
    "This is Walid Hassan's certifications page of the portfolio website"
};

const CertificationsLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default CertificationsLayout;
